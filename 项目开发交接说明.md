# 个人经营工作台：项目开发交接说明

> 更新时间：2026-09-07（Asia/Shanghai）  
> 用途：更换电脑后快速恢复开发环境，并继续下一阶段开发。

## 1. 项目位置与线上信息

| 项目 | 位置 |
| --- | --- |
| 当前电脑源码 | `C:\Users\Administrator\Documents\ChatGPT\个人工作站` |
| GitHub 仓库 | `https://github.com/onlylm/work.git` |
| 主分支 | `main` |
| 当前源码提交 | `2385f6f`（修复共享网络数据库主机名冲突） |
| 服务器源码 | `/opt/personal-workbench` |
| 服务器 | `45.197.146.165`，SSH 端口 `7961` |
| 公网地址 | `https://www.bugan.cn` |
| 本机监听 | `127.0.0.1:8790` |
| 健康检查 | `https://www.bugan.cn/api/health` |

截至本文更新时间，线上健康检查返回：

```json
{
  "ok": true,
  "database": "ready",
  "service": "personal-business-workbench",
  "version": "0.2.1"
}
```

线上目前运行的是已经修复数据库连接的 `0.2.1` 镜像；GitHub 最新源码中的健康检查标识已更新为 `0.2.2`，下次重新构建镜像后会显示新标识。

服务器上还运行着 `shuku` 和 `NIMAIL`。三个项目共用既有 Caddy 的 80/443 端口，工作台不可再单独占用 80/443。

## 2. 技术栈

- Next.js `16.3.4`（App Router、Server Components、Server Actions）
- React `19.2.8`
- TypeScript 5
- Tailwind CSS 4 与项目自定义全局样式
- PostgreSQL 17
- Drizzle ORM `0.45.2` / Drizzle Kit `0.31.10`
- Zod 4 表单校验
- bcryptjs 密码哈希
- jose JWT 会话
- Vitest 5
- Docker Compose + Caddy
- Node.js 24 Alpine（生产镜像）
- pnpm `11.19.0`

重要：本项目的 Next.js 版本包含破坏性变更。修改 Next.js 代码前，必须先阅读 `node_modules/next/dist/docs/` 中与任务有关的文档，并遵守根目录 `AGENTS.md`。

## 3. 目录说明

```text
个人工作站/
├─ src/app/
│  ├─ (app)/                 登录后页面与业务 Server Actions
│  ├─ api/health/            应用和数据库健康检查
│  ├─ login/                 登录页面、登录与退出动作
│  ├─ globals.css            全站布局、组件和响应式样式
│  ├─ layout.tsx             根布局和元信息
│  └─ page.tsx               根路径跳转入口
├─ src/components/
│  ├─ app-shell.tsx          侧边栏、移动端抽屉、用户菜单
│  ├─ quick-entry.tsx        中文快速记录解析与确认
│  └─ ui.tsx                 通用页面标题、空状态等组件
├─ src/db/
│  ├─ index.ts               PostgreSQL 连接
│  └─ schema.ts              Drizzle 数据库结构
├─ src/lib/
│  ├─ auth.ts                JWT Cookie、会话校验
│  ├─ bootstrap.ts           首次访问时初始化管理员
│  ├─ finance.ts             美元移动加权成本计算
│  ├─ finance.test.ts        当前自动化测试
│  └─ format.ts              金额、时间、状态格式化
├─ drizzle/                  已提交的数据库迁移及快照
├─ deploy/
│  ├─ install.sh             一键部署和升级脚本
│  ├─ backup.sh              PostgreSQL 每日备份脚本
│  └─ Caddyfile.example      独立配置参考
├─ docker-compose.yml        app、migrate、db、backup 服务
├─ Dockerfile                依赖、构建、运行三阶段镜像
├─ drizzle.config.ts
├─ package.json
└─ README.md
```

## 4. 当前已实现功能

### 4.1 登录与安全

- 根据服务器 `.env` 中的 `ADMIN_EMAIL`、`ADMIN_PASSWORD` 自动初始化单一管理员。
- 管理员密码使用 bcrypt（cost 12）哈希后写入数据库，数据库不保存明文密码。
- 使用 JWT 签名的 HttpOnly、SameSite=Lax Cookie，会话有效期 7 天。
- 生产环境 Cookie 仅通过 HTTPS 发送。
- 同一邮箱 15 分钟内最多失败 5 次，失败记录写入 `login_attempts`。
- `(app)` 下所有业务页面都通过 `requireSession()` 校验登录。
- 所有写入 Server Action 再次独立校验会话和 `workspace_id`。
- 未登录访问 `/dashboard` 等业务页面会跳转 `/login`。
- 关键新增动作和待办状态变化写入 `audit_logs`。
- 登录页不会向公网显示底层数据库错误原文。

### 4.2 工作台首页

- 今日收款。
- 今日已核算利润。
- 今日经营支出。
- 客户总数。
- 今日最近业务列表。
- 未完成待办列表。
- 当前美元余额、人民币成本余额和移动平均成本。
- 首页已移除演示数据和不必要的密集模块。

### 4.3 客户管理

- 新增客户：姓名、手机号、微信号、备注。
- 查看真实客户列表。
- 数据按工作空间隔离。
- `last_business_at` 字段已存在，但新增业务时尚未自动更新，列入后续任务。

### 4.4 业务流水

- 新增并查看最近 100 条业务。
- 支持会员、账号产品、实物产品、服务、其他五种业务类型。
- 支持待付款、已付款待办理、办理中、已完成、售后、已退款状态。
- 可关联客户，记录业务时间、收款、成本和直接支出。
- 成本为空时利润保持“待核算”，不会按零成本虚增利润。
- 自动计算已知成本业务的利润。
- 收款大于 0 时自动生成关联的人民币资金流水。
- 业务编号使用时间戳和随机后缀避免并发冲突。

### 4.5 资金管理

- 记录经营支出、采购、退款、其他收入和其他支出。
- 查看最近 100 条人民币流水。
- 记录美元充值和美元消费。
- 计算美元余额、人民币成本余额和移动加权平均成本。
- 美元流水写入时使用 PostgreSQL 事务级 advisory lock，避免并发错账。

### 4.6 商品、待办、网站、报表

- 商品：新增名称、类型、分类、默认售价、默认成本、安全库存。
- 待办：新增、标记完成、重新打开。
- 常用网站：新增并打开自建或第三方网站，只保存入口，不保存第三方密码。
- 快速记录：本地规则解析中文描述中的业务类型、收款和成本，预览确认后写入。
- 会员页面：筛选并显示会员类型业务。
- 报表：累计收款、累计成本、已核算利润、客户数和按业务类型汇总。
- 页面使用真实 PostgreSQL 数据，不包含硬编码演示数据。

### 4.7 部署与运维

- Docker Compose 管理应用、迁移、PostgreSQL 和备份容器。
- `install.sh` 自动拉取 `main`、构建镜像、启动数据库、强制迁移、验证表结构、启动应用并检查健康状态。
- 自动发现服务器现有 Caddy，共用现有 80/443，不影响 `shuku`、`NIMAIL`。
- 工作台应用仅绑定 `127.0.0.1:8790`。
- PostgreSQL 使用 Docker 数据卷 `workbench_db`。
- 每日执行一次 `pg_dump`，备份保留 14 天，保存于 `workbench_backups` 数据卷。
- 数据库使用专用网络别名 `personal-workbench-db`。不要改回通用的 `db`，否则应用加入共享 Caddy 网络后可能连接到其他项目的数据库。

## 5. 尚未完成或只完成基础结构的功能

以下内容不能当作已经可用：

- 客户、业务、商品、网站、资金流水的编辑和删除。
- 列表搜索、筛选、分页和批量操作。
- 商品真实库存数量、入库批次、出库、盘点和库存流水。
- 账号、CDK、卡密等敏感库存的加密导入、原子领取、一次性展示和自动发货。
- `inventory_items` 表已有基础结构，但交付页面主动保持禁用。
- `customer_accounts` 表已有基础结构，但没有管理页面和 Server Action。
- 会员套餐、充值时间、开始时间、到期时间、续费历史和自动提醒。
- 商品与业务的正式关联；`product_id` 字段已存在，但当前业务表单未使用。
- 客户最近业务时间自动更新。
- 报表日期范围、日/月趋势、导出和图表。
- 管理员修改密码、找回密码、多管理员和角色权限。
- 数据导入、导出、备份下载和可视化恢复界面。
- `shuku`、`NIMAIL` API 数据联动。目前只是服务器部署隔离，没有业务接口集成。
- 更完整的自动化测试。目前只有美元资金计算的 4 个单元测试，缺少数据库集成测试和浏览器端到端测试。

账号密码、2FA、卡密等敏感信息在加密交付模块完成前不要录入系统。

## 6. 数据库结构

| 表 | 作用 | 当前使用状态 |
| --- | --- | --- |
| `workspaces` | 工作空间 | 已使用 |
| `users` | 管理员账号和密码哈希 | 已使用 |
| `workspace_members` | 用户与工作空间关系 | 已使用 |
| `login_attempts` | 登录失败限流 | 已使用 |
| `customers` | 客户资料 | 已使用 |
| `customer_accounts` | 客户平台账号 | 仅有结构 |
| `products` | 商品基础资料 | 已使用基础字段 |
| `business_records` | 统一业务流水 | 已使用 |
| `finance_transactions` | 人民币资金流水 | 已使用 |
| `usd_fund_transactions` | 美元资金及成本流水 | 已使用 |
| `tasks` | 待办事项 | 已使用 |
| `websites` | 常用网站 | 已使用基础字段 |
| `inventory_items` | 加密账号/卡密库存 | 仅有结构，页面禁用 |
| `audit_logs` | 操作审计 | 已使用基础功能 |

迁移文件：

- `drizzle/0000_broken_aqueduct.sql`：初始完整结构。
- `drizzle/0001_steep_senator_kelly.sql`：人民币资金流水。
- `drizzle/0002_violet_magma.sql`：登录失败记录。

修改 `src/db/schema.ts` 后必须运行：

```bash
pnpm db:generate
```

检查新迁移 SQL 后再运行：

```bash
pnpm db:migrate
```

不要修改已经在线上执行过的旧迁移文件，应新增迁移。

## 7. 换电脑后的接续步骤

### 7.1 安装基础工具

新电脑需要：

- Git
- Node.js 24
- Corepack / pnpm 11.19.0
- PostgreSQL（本地运行开发时需要）或 Docker Desktop

### 7.2 克隆项目

```powershell
git clone https://github.com/onlylm/work.git
cd work
git switch main
git pull --ff-only origin main
```

确认当前代码：

```powershell
git log -1 --oneline
git status
```

### 7.3 安装依赖

```powershell
corepack enable
pnpm install --frozen-lockfile
```

不要删除或重新生成 `pnpm-lock.yaml`，除非依赖确实发生变化。

### 7.4 创建本地环境变量

复制 `.env.example` 为 `.env`：

```powershell
Copy-Item .env.example .env
```

然后替换其中的本地数据库密码、管理员密码和随机密钥。严禁把 `.env` 提交 GitHub。

### 7.5 准备本地数据库

创建 PostgreSQL 数据库和用户后，确保 `.env` 中的 `DATABASE_URL` 可以连接，再执行：

```powershell
pnpm db:migrate
pnpm dev
```

默认开发地址通常是 `http://localhost:3000`。

### 7.6 每次提交前检查

```powershell
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

建议新功能至少补充相应单元测试；涉及登录、业务和资金写入时，应补数据库集成测试。

## 8. Git 开发约定

- 生产分支为 `main`。
- 新功能建议使用 `codex/功能名` 或 `feature/功能名` 分支。
- 提交前先检查 `git status`，不要覆盖其他未提交修改。
- 不提交 `.env`、数据库文件、备份、`.next` 或 `node_modules`。
- 数据库结构变化必须同时提交 schema、SQL migration 和 meta 快照。
- 禁止对生产仓库执行 `git reset --hard`。
- 禁止删除生产数据卷 `workbench_db`。

常用流程：

```powershell
git switch -c codex/feature-name
# 开发并完成检查
git add <本次相关文件>
git commit -m "feat: 功能说明"
git push -u origin codex/feature-name
```

## 9. 服务器部署与排错

### 9.1 推荐升级命令

服务器此前访问 `registry.npmmirror.com` 出现长时间停滞，部署时优先使用 npm 官方源：

```bash
curl -fsSL https://raw.githubusercontent.com/onlylm/work/main/deploy/install.sh \
  | env WORKBENCH_DOMAIN=www.bugan.cn NPM_REGISTRY=https://registry.npmjs.org bash
```

脚本会保留已有 `.env` 和 PostgreSQL 数据卷。

### 9.2 服务器手工检查

```bash
cd /opt/personal-workbench
docker compose -f docker-compose.yml -f docker-compose.caddy.yml ps
docker compose -f docker-compose.yml -f docker-compose.caddy.yml logs --tail=100 app migrate db
curl http://127.0.0.1:8790/api/health
curl https://www.bugan.cn/api/health
```

健康检查必须同时包含：

```json
{"ok":true,"database":"ready"}
```

### 9.3 查看管理员账号

只读取需要的字段，不要输出完整 `.env`：

```bash
sed -n 's/^ADMIN_EMAIL=//p' /opt/personal-workbench/.env
sed -n 's/^ADMIN_PASSWORD=//p' /opt/personal-workbench/.env
```

### 9.4 数据保护

部署和升级不得执行：

```text
docker compose down -v
docker volume rm ...workbench_db
rm -rf /opt/personal-workbench
```

在做任何可能影响数据库的操作前，先确认备份存在。密码、数据库密码、JWT 密钥和加密密钥不得写入开发文档、截图或聊天记录。

## 10. 下一阶段建议计划

### P0：补齐现有模块闭环

1. 为客户、业务、商品、网站和资金流水增加详情、编辑、归档/删除与二次确认。
2. 新增业务时同步更新客户 `last_business_at`。
3. 业务表单接入 `product_id`，选择商品后自动带出默认售价和成本。
4. 增加统一成功/失败提示，避免 Server Action 校验失败时只显示通用错误页。
5. 增加列表搜索、日期筛选和分页。

验收要求：所有写操作校验 `workspace_id`、记录审计日志，并有数据库集成测试。

### P1：会员管理

1. 新增会员订单或会员订阅表，记录平台、套餐、开始时间、到期时间、成本和售价。
2. 支持续费并保留历史记录。
3. 自动生成到期前 7 天、3 天、1 天待办。
4. 首页只显示近期到期提醒，完整数据进入会员模块。

### P1：库存与安全交付

1. 增加库存批次、库存流水和实物库存盘点。
2. 账号/CDK/卡密使用服务端加密密钥加密，增加去重哈希。
3. 领取库存必须在事务中原子锁定，防止同一条库存被重复发货。
4. 敏感内容默认隐藏，只允许一次性查看并记录审计。
5. 完成安全评审和并发测试前，不开放交付页面。

### P2：外部接口集成

1. 先整理 `shuku` 和 `NIMAIL` 的接口文档、鉴权方式、服务地址与最小权限。
2. 服务端调用外部接口，不把 API 密钥发送到浏览器。
3. 设置超时、重试、幂等键和失败补偿。
4. 外部服务不可用时不能阻塞工作台的基础记账功能。

### P2：运维与质量

1. 增加管理员修改密码和会话全部失效能力。
2. 增加 CSV/Excel 导入导出。
3. 增加备份列表、下载和恢复演练说明。
4. 增加 Playwright 端到端测试和生产部署冒烟测试。
5. 考虑由 GitHub Actions 构建镜像，服务器只拉取镜像，减少服务器 npm 网络问题。

## 11. 新电脑开始开发前的检查清单

- [ ] 已从 `https://github.com/onlylm/work.git` 克隆最新 `main`。
- [ ] 已阅读 `AGENTS.md` 和相关 Next.js 本地文档。
- [ ] `.env` 只使用本地测试密钥，且未被 Git 跟踪。
- [ ] 本地数据库迁移成功。
- [ ] 能登录并访问 `/dashboard`。
- [ ] `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm build` 全部通过。
- [ ] 开发前已创建独立功能分支。
- [ ] 没有把生产服务器密码或生产 `.env` 复制到新电脑仓库。

