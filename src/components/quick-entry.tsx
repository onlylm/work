"use client";
import {useMemo,useState} from "react";import {CheckCircle2,Sparkles} from "lucide-react";import {createBusiness} from "@/app/(app)/actions";import {businessTypeName} from "@/lib/format";
function amount(raw:string,patterns:RegExp[]){for(const p of patterns){const m=raw.match(p);if(m)return m[1]}return""}
export function QuickEntry(){const[raw,setRaw]=useState("");const[preview,setPreview]=useState(false);const parsed=useMemo(()=>{let type="other";if(/续费|充值.*(?:会员|Plus|Pro)|会员/.test(raw))type="membership";else if(/账号|卡密|成品号/.test(raw))type="account";else if(/流量卡|实物|购买了/.test(raw))type="physical";else if(/安装|服务|咨询/.test(raw))type="service";const revenue=amount(raw,[/(?:收|收费|收到|卖了|卖给.+?收到)\s*(\d+(?:\.\d+)?)\s*元/,/(\d+(?:\.\d+)?)\s*元(?:收款|到账)/]);const cost=amount(raw,[/(?:成本|使用|花费|购买)\s*(\d+(?:\.\d+)?)\s*元/]);return{type,revenue,cost}},[raw]);return <div className="quick-entry"><section className="quick-compose"><span><Sparkles/></span><div><h3>用一句话记录业务</h3><p>文字先在浏览器中解析，确认后才发送到服务器写入。</p><textarea value={raw} onChange={e=>{setRaw(e.target.value);setPreview(false)}} placeholder="例如：今天给李明安装AI软件，收费300元。"/><button disabled={!raw.trim()} onClick={()=>setPreview(true)}>解析并预览</button></div></section>{preview&&<section className="preview-card"><header><CheckCircle2/><div><h3>请确认识别结果</h3><p>关键金额无法识别时请返回修改，不会自动猜测。</p></div></header><dl><div><dt>业务类型</dt><dd>{businessTypeName[parsed.type]}</dd></div><div><dt>收款金额</dt><dd>{parsed.revenue?`¥${parsed.revenue}`:"未识别"}</dd></div><div><dt>成本金额</dt><dd>{parsed.cost?`¥${parsed.cost}`:"待核算"}</dd></div><div className="full"><dt>业务内容</dt><dd>{raw}</dd></div></dl><form action={createBusiness}><input type="hidden" name="customerId" value=""/><input type="hidden" name="type" value={parsed.type}/><input type="hidden" name="content" value={raw}/><input type="hidden" name="revenue" value={parsed.revenue||"0"}/><input type="hidden" name="cost" value={parsed.cost}/><input type="hidden" name="directExpense" value="0"/><input type="hidden" name="status" value="paid_pending"/><button type="button" onClick={()=>setPreview(false)}>返回修改</button><button className="main-button" disabled={!parsed.revenue}>确认记录</button></form></section>}</div>}
