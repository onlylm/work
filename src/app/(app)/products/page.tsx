import {desc,eq} from "drizzle-orm";import {getDb} from "@/db";import {products} from "@/db/schema";import {requireSession} from "@/lib/auth";import {businessTypeName,cny} from "@/lib/format";import {Empty,PageHeader} from "@/components/ui";import {createProduct} from "../actions";
export const dynamic="force-dynamic";
export default async function Products(){const s=await requireSession();const rows=await getDb().select().from(products).where(eq(products.workspaceId,s.workspaceId)).orderBy(desc(products.createdAt));return <div className="page"><PageHeader title="商品与库存" description="维护会员、账号、实物及服务商品的基础资料"/><details className="form-disclosure"><summary>＋ 新增商品</summary><form action={createProduct} className="data-form"><label>商品名称<input name="name" required/></label><label>商品类型<select name="type"><option value="membership">会员充值</option><option value="account">账号产品</option><option value="physical">实物产品</option><option value="service">服务</option><option value="other">其他</option></select></label><label>分类<input name="category"/></label><label>默认售价（元）<input name="price" type="number" min="0" step="0.01" defaultValue="0"/></label><label>默认成本（元）<input name="cost" type="number" min="0" step="0.01" defaultValue="0"/></label><label>安全库存<input name="safetyStock" type="number" min="0" step="1" defaultValue="0"/></label><button>保存商品</button></form></details><section className="content-card list-card">{rows.length?<div className="data-table"><table><thead><tr><th>商品名称</th><th>类型</th><th>分类</th><th>默认售价</th><th>默认成本</th><th>安全库存</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><b>{x.name}</b></td><td>{businessTypeName[x.type]}</td><td>{x.category||"—"}</td><td>{cny(x.defaultPriceCents)}</td><td>{cny(x.defaultCostCents)}</td><td>{x.safetyStock}</td></tr>)}</tbody></table></div>:<Empty title="还没有商品" description="先建立商品，再进行库存入库和业务关联"/>}</section></div>}
