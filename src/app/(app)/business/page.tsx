import {desc,eq} from "drizzle-orm";import {getDb} from "@/db";import {businessRecords,customers} from "@/db/schema";import {requireSession} from "@/lib/auth";import {businessStatusName,businessTypeName,cny,datetime} from "@/lib/format";import {Empty,PageHeader} from "@/components/ui";import {createBusiness} from "../actions";
export const dynamic="force-dynamic";
export default async function Business(){const s=await requireSession();const db=getDb();const [rows,clientRows]=await Promise.all([db.select().from(businessRecords).where(eq(businessRecords.workspaceId,s.workspaceId)).orderBy(desc(businessRecords.occurredAt)).limit(100),db.select().from(customers).where(eq(customers.workspaceId,s.workspaceId)).orderBy(customers.name)]);return <div className="page"><PageHeader title="业务流水" description="业务只录一次，统一保存收款、成本、利润与状态"/><details className="form-disclosure"><summary>＋ 记录新业务</summary><form action={createBusiness} className="data-form"><label>客户<select name="customerId"><option value="">未关联客户</option>{clientRows.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>业务类型<select name="type"><option value="membership">会员充值</option><option value="account">账号产品</option><option value="physical">实物产品</option><option value="service">服务</option><option value="other">其他</option></select></label><label className="wide">业务内容<input name="content" required maxLength={500}/></label><label>收款（元）<input name="revenue" type="number" min="0" step="0.01" defaultValue="0" required/></label><label>成本（元，可留空）<input name="cost" type="number" min="0" step="0.01"/></label><label>直接支出（元）<input name="directExpense" type="number" min="0" step="0.01" defaultValue="0"/></label><label>业务时间<input name="occurredAt" type="datetime-local"/></label><label>状态<select name="status"><option value="paid_pending">已付款待办理</option><option value="pending_payment">待付款</option><option value="processing">办理中</option><option value="completed">已完成</option><option value="after_sale">售后</option><option value="refunded">已退款</option></select></label><button>确认并记录</button></form></details><section className="content-card list-card"><header><div><h3>全部业务</h3><p>最近 100 条记录</p></div></header>{rows.length?<div className="data-table"><table><thead><tr><th>编号</th><th>内容</th><th>类型</th><th>收款</th><th>成本</th><th>利润</th><th>状态</th><th>时间</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.number}</td><td><b>{x.content}</b></td><td>{businessTypeName[x.type]}</td><td>{cny(x.revenueCents)}</td><td>{cny(x.costCents)}</td><td>{cny(x.profitCents)}</td><td><span className="tag">{businessStatusName[x.status]}</span></td><td>{datetime(x.occurredAt)}</td></tr>)}</tbody></table></div>:<Empty title="还没有业务流水" description="录入首笔业务后，资金和利润会自动计算"/>}</section></div>}
